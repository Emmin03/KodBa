<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="JobsNestApp.Pages.SignupPage"
             BackgroundColor="{StaticResource BackgroundColor}">
    <Grid RowDefinitions="Auto, *, Auto" Padding="20">
        <Label Grid.Row="0" Text="Registracija" FontSize="30" HorizontalOptions="Center" TextColor="{StaticResource PrimaryColor}" Margin="0,20,0,0" />
        <VerticalStackLayout Grid.Row="1" Spacing="15" VerticalOptions="Center">
            <Frame CornerRadius="10" Padding="10" BackgroundColor="White" HasShadow="True">
                <VerticalStackLayout Spacing="10">
                    <Entry x:Name="UsernameEntry" Placeholder="Korisničko Ime" PlaceholderColor="Gray" />
                    <Entry x:Name="EmailEntry" Placeholder="Email" PlaceholderColor="Gray" />
                    <Entry x:Name="PasswordEntry" Placeholder="Lozinka" IsPassword="True" PlaceholderColor="Gray" />
                </VerticalStackLayout>
            </Frame>
            <Label x:Name="ErrorLabel" TextColor="Red" FontSize="14" />
        </VerticalStackLayout>
        <Button Grid.Row="2" Text="Registriraj se" Clicked="OnSignupClicked" BackgroundColor="{StaticResource PrimaryColor}" TextColor="White" CornerRadius="10" />
    </Grid>
</ContentPage>